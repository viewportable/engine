{
  "name": "Viewportable",
  "webhookPath": "/github/webhooks",
  "repositoryPermissions": {
    "metadata": "read",
    "contents": "read",
    "pull_requests": "write",
    "checks": "write"
  },
  "events": ["pull_request"],
  "notes": [
    "installation and installation_repositories lifecycle events are consumed by default",
    "Cloud mode uses the GitHub App as the only Viewportable Engine Check writer",
    "candidate executors never receive App private keys or installation access tokens"
  ]
}
